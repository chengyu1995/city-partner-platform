# CI 状态: ✅ 绿了

**最新 CI: ✅ `39e0c4d` (5 min ago)**
**上次: ✅ `159d120` (8 min ago)**

之前看到的 ❌ 是更早的 run (`acbcc1c`/`16ed07c`/`6ccb8af`) — 那些 commit 是 fix 的中间过程, 现在都修了.

## Status check name

`Linting and Build (build)` 或简称 `build`

## 你可以配了

1. https://github.com/chengyu1995/city-partner-platform/settings/branches
2. main 规则 → "Require status checks to pass before merging" 段
3. 搜 `build` 勾上
4. 保存

## Vercel 删除 -tfpf

同时: https://vercel.com/dashboard → 选 `-tfpf` 项目 → Settings → General → 底部 Delete
